# TodoList contract address
# Get it from: bunx hardhat run scripts/deploy.ts --network localhost
# (printed to console after deploy, also saved here automatically)
NEXT_PUBLIC_CONTRACT_ADDRESS=

# TodoNFT contract address
# Get it from: bunx hardhat run scripts/deploy.ts --network localhost
# (printed to console after deploy, also saved here automatically)
NEXT_PUBLIC_NFT_ADDRESS=
